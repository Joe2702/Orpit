{
  "name": "orbit",
  "version": "2.0.0",
  "private": true,
  "description": "Orbit — full-stack life tracker (React + Node + Postgres)",
  "scripts": {
    "build": "npm --prefix web install && npm --prefix web run build && npm --prefix server install && npm --prefix server run build",
    "start": "npm --prefix server run start",
    "migrate": "npm --prefix server run migrate"
  }
}
